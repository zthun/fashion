import type { ReactNode } from "react";

/**
 * Represents a component that has a built in label.
 */
export interface IZComponentLabel {
  /**
   * The built in label node.
   */
  label?: ReactNode;
}
